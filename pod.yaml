apiVersion: v1
kind: Deployment
metadata:
  name: my-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        env: dev
    spec:
      containers:
      - name: x-container
        image: vijaykmm/docker-k8s
        ports:
        - containerPort: 80
