apiVersion: extensions/v1beta1
kind: deployment
metadata:
  name: my-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        env: dev
    spec:
      containers:
      - name: x-container
        image: vijaykmm/docker-k8s:tagname
        ports:
        - containerPort: 8080
