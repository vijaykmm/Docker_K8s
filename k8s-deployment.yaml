apiVersion: v1
kind: Deployment
metadata:
  name: my-nginx
  labels:
    app: nginx
    spec:
      containers:
      - name: nginx
        image: vijaykmm/docker-k8s
        ports:
        - containerPort: 80
